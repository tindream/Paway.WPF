<Page x:Class="Paway.Test.TestPlotView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Paway.Test"
      xmlns:model="clr-namespace:Paway.Model;assembly=Paway.Model"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
      xmlns:oxy="http://oxyplot.org/wpf"
      mc:Ignorable="d" 
      UseLayoutRounding="True"
      DataContext="{Binding TestPlotView, Source={StaticResource Locator}}"
      d:DesignHeight="450" d:DesignWidth="600"
      Title="TestPlotView">
    <Border BorderBrush="LightGray" Margin="20" Background="{StaticResource ThemeBackground}">
        <oxy:PlotView x:Name="plotView" Model="{Binding PlotModel}">
            <oxy:PlotView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="重置" Padding="5" FontSize="15" Command="{Binding Reset}"/>
                </ContextMenu>
            </oxy:PlotView.ContextMenu>
        </oxy:PlotView>
    </Border>
</Page>
