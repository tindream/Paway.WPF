<Page x:Class="Paway.Test.TestAnimation"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Paway.Test"
      xmlns:model="clr-namespace:Paway.Model;assembly=Paway.Model"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
      mc:Ignorable="d" 
      UseLayoutRounding="True"
      d:DesignHeight="450" d:DesignWidth="600"
      Title="TestAnimation">
    <Border BorderBrush="LightGray" Margin="20">
        <StackPanel>
            <!-- 3D旋转卡片 -->
            <Border Width="200" Height="120" Background="{StaticResource Color}" CornerRadius="10" Margin="20">
                <Border.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Angle" From="0" To="360" Duration="0:0:8"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
                <Border.RenderTransform>
                    <RotateTransform CenterX="100" CenterY="60"/>
                </Border.RenderTransform>
                <TextBlock Text="3D旋转" Foreground="White" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- 弹跳缩放圆形 -->
            <Ellipse Width="60" Height="60" Fill="#FF00C853" Margin="20">
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="MouseDown">
                        <BeginStoryboard>
                            <Storyboard>
                                <!-- 先缩小 -->
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" From="1" To="0.8" Duration="0:0:0.1" />
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" From="1" To="0.8" Duration="0:0:0.1"/>

                                <!-- 弹弹跳恢复（核心！） -->
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.4">
                                    <DoubleAnimation.EasingFunction>
                                        <BounceEase Bounces="2" Bounciness="3"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>

                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.4">
                                    <DoubleAnimation.EasingFunction>
                                        <BounceEase Bounces="2" Bounciness="3"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>

                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
                <Ellipse.RenderTransform>
                    <ScaleTransform CenterX="30" CenterY="30"/>
                </Ellipse.RenderTransform>
            </Ellipse>
            
        </StackPanel>
    </Border>
</Page>
