<WindowEXT x:Class="Paway.Test.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paway.Test"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" 
        xmlns:ip="http://www.galasoft.ch/mvvmlight"
        mc:Ignorable="d"
        UseLayoutRounding="True"
        DataContext="{Binding Test, Source={StaticResource Locator}}"
        Title="TestWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <WindowEXT.FunctionBar>
        <local:TitleContent/>
    </WindowEXT.FunctionBar>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center">
            <SliderEXT Minimum="-40" Maximum="20" Value="{Binding Value2}"/>
            <Grid>
                <Menu Padding="2,0,0,2" DockPanel.Dock="Top" Height="33">
                    <MenuItem Header="升级">
                        <MenuItem Header="升级"/>
                        <MenuItem Header="升级"/>
                    </MenuItem>
                    <MenuItem Header="设置"/>
                </Menu>
                <Border BorderThickness="0,0,0,2" BorderBrush="{StaticResource ThemeHigh}"/>
            </Grid>
            <ButtonEXT Click="ButtonEXT_Click"/>
            <ListViewCustom ItemsSource="{Binding GridList}" Name="listView">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding SelectionDeviceCommand}" CommandParameter="{Binding ElementName=listView}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ListViewCustom>
        </StackPanel>
    </Grid>
</WindowEXT>
