<WindowEXT x:Class="Paway.Test.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paway.Test"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" 
        xmlns:ip="http://www.galasoft.ch/mvvmlight"
        mc:Ignorable="d"
        UseLayoutRounding="True"
        DataContext="{Binding Test, Source={StaticResource Locator}}"
        Title="TestWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <WindowEXT.FunctionBar>
        <local:TitleContent/>
    </WindowEXT.FunctionBar>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center">
            <WrapPanel Orientation="Vertical" Height="34">
                <RadioBox Content="N1" GroupName="MikeMode" Margin="2,2,0,2" Radius="3,0,0,3"
                                  IsChecked="{Binding UserType, Converter={StaticResource valueToTrue}, ConverterParameter=1}"/>
                <RadioBox Content="N2" GroupName="MikeMode" Margin="-1,2,0,2" Radius="0" HorizontalContentAlignment="Center"
                                  IsChecked="{Binding UserType, Converter={StaticResource valueToTrue}, ConverterParameter=2}"/>
                <RadioBox Content="N3" GroupName="MikeMode" Margin="-1,2,0,2" Radius="0,3,3,0"
                                  IsChecked="{Binding UserType, Converter={StaticResource valueToTrue}, ConverterParameter=4}"/>
            </WrapPanel>
            <!--<ListViewCustom ItemsSource="{Binding GridList}" INormal="True"/>-->
            <ButtonEXT Click="ButtonEXT_Click" Command="{Binding ButtonCommand}"/>
            <WrapPanel Orientation="Vertical" Height="34">
                <RadioBox Content="N1" GroupName="MikeMode2" Margin="2,2,0,2" Radius="3,0,0,3"
                                  IsChecked="{Binding UserType, Converter={StaticResource valueToTrue}, ConverterParameter=1}"/>
                <RadioBox Content="N2" GroupName="MikeMode2" Margin="-1,2,0,2" Radius="0" HorizontalContentAlignment="Center"
                                  IsChecked="{Binding UserType, Converter={StaticResource valueToTrue}, ConverterParameter=2}"/>
                <RadioBox Content="N3" GroupName="MikeMode2" Margin="-1,2,0,2" Radius="0,3,3,0"
                                  IsChecked="{Binding UserType, Converter={StaticResource valueToTrue}, ConverterParameter=4}"/>
            </WrapPanel>
        </StackPanel>
        <Border Grid.Column="1" Background="Transparent">
            <!--<DataGridEXT ItemsSource="{Binding GridList}" HorizontalContentAlignment="Center"/>-->
        </Border>
    </Grid>
</WindowEXT>
