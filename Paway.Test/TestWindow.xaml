<WindowEXT x:Class="Paway.Test.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Paway.Test"
        mc:Ignorable="d"
        UseLayoutRounding="True"
        DataContext="{Binding Test, Source={StaticResource Locator}}"
        Title="TestWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="211"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Border Name="borderClip" BorderBrush="Gray" BorderThickness="1" Background="Green" Width="100" Height="100">
            <Border.Clip>
                <RectangleGeometry  Rect="0,10,100,2"/>
            </Border.Clip>
        </Border>

        <Border Name="borderLine" Grid.Column="1" BorderBrush="LightGray" BorderThickness="1"  Margin="20">
            <Canvas Name="canvas" Grid.Column="1" Background="White">
                <!--文本框-->
                <Border Name="borderText" Canvas.Left="186" Canvas.Top="82" Background="Transparent"
                        BorderBrush="DarkKhaki" BorderThickness="1"  Padding="5"
                        Height="94" Width="198" MinHeight="10" MinWidth="10">
                    <!--需要Label/textblock的文字大小随resize改变，则使用Viewbox，否则，去掉Viewbox-->
                    <!--https://stackoverflow.com/questions/1464185/how-to-set-textblock-or-label-with-resizable-font-size-in-wpf-->
                    <Viewbox TextOptions.TextFormattingMode="Ideal">
                        <TextBlock Name="myTextBlock" Text="move and rezise TextBlock" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
                    </Viewbox>
                </Border>
            </Canvas>
        </Border>
    </Grid>
</WindowEXT>
